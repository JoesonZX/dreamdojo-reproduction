# Contrastive v3: PK sampler by VERB (scene-independent action label) instead of task.
#  - data.pk_label: verb -> same-verb positives span many scenes (task=scene in EgoDex)
#  - model.beta 1e-4: gentle KL to bound ||z_mu|| (v1 drifted: train KL hit ~143)
#  - model.contrastive_warmup: ramp supcon+temporal so the action head settles first
# Full fine-tune from official LAM_400k, latent=40 (32 action + 8 env), 1000 steps.
model:
  image_channels: 3
  lam_model_dim: 1024
  lam_latent_dim: 40
  lam_patch_size: 16
  lam_enc_blocks: 24
  lam_dec_blocks: 24
  lam_num_heads: 16
  lam_dropout: 0.0
  beta: 1.0e-4              # bound latent norm (was 1e-6 -> drift)
  env_dim: 8
  action_dim: 18
  action_head: true
  lambda_action: 1.0
  lambda_indep: 0.0
  contrastive: true
  proj_dim: 64
  lambda_supcon: 0.3
  lambda_temporal: 0.3
  supcon_tau: 0.1
  contrastive_warmup: 500
  init_from: /home/xuan/embodied-ai/checkpoints/pretrained/LAM_400k.ckpt
  freeze_encoder: false
  log_path: /home/xuan/embodied-ai/checkpoints/lam-dis/ft_l40_contrastive_v3_5k/log_imgs

data:
  dataset_type: egodex
  data_root: /home/xuan/embodied-ai/data/egodex/test_240p
  img_h: 240
  img_w: 320
  downsample_factors: [1, 2]
  val_ratio: 0.1
  seed: 42
  load_actions: true
  pk_sampler: true         # class-balanced batches
  pk_label: verb           # group/contrast by scene-independent action verb
  load_verbs: true
  classes_per_batch: 2     # P (micro-batch = P*K = 8)
  samples_per_class: 4     # K -> 3 positives + 4 negatives per anchor

training:
  per_gpu_batch_size: 8    # ignored under pk_sampler (batch = P*K); kept for clarity
  gradient_accumulation_steps: 2   # eff batch 16
  num_workers: 10
  pin_memory: true
  total_steps: 5000
  lr: 1.0e-5
  weight_decay: 0.01
  warmup_steps: 200
  mixed_precision: bf16
  output_dir: /home/xuan/embodied-ai/checkpoints/lam-dis/ft_l40_contrastive_v3_5k
  save_every: 1000
  eval_every: 500
  log_every: 50
  keep_last_n: 1

logging:
  project: dreamdojo-lam-disentangle
  run_name: ft-l40-contrastive-v3-5k
  use_wandb: false
